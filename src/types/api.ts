export type Organization = { id: string; name: string; creditBalanceMicros: string; role: 'OWNER' | 'ADMIN' | 'EDITOR' | 'VIEWER' };
export type Me = { profile: { id: string; email: string; displayName?: string; platformRole: 'USER' | 'PLATFORM_ADMIN' }; organizations: Organization[] };
export type Site = { id: string; name: string; domain: string; language: string; wordpressStatus: string; wordpressUser?: string; wordpressVerifiedAt?: string; publishPolicy: string; manualPublishSuccesses: number; autoPublishEnabledAt?: string; integrations: Array<{ id: string; provider: 'GSC'; propertyId?: string; status: string; lastSyncedAt?: string; lastErrorMessage?: string }> };
export type KnowledgeSource = { id: string; siteId?: string; type: string; title: string; summary?: string; status: string; createdAt: string };
export type Opportunity = { id: string; siteId: string; keyword: string; searchVolume: number; keywordDifficulty: number; allintitleCount: number; roiScoreMicros: string; formulaVersion: string; status: string };
export type JobRun = { id: string; type: string; status: string; errorMessage?: string; createdAt: string; finishedAt?: string };
export type Draft = { id: string; siteId: string; title: string; status: string; html: string; qualityReport: { passed?: boolean; score?: number }; publishedUrl?: string; reviews: Array<{ decision: string; comment?: string }>; publishAttempts: Array<{ status: string; remoteUrl?: string; errorMessage?: string }> };
export type Ledger = { balanceMicros: string; heldMicros: string; availableMicros: string; entries: Array<{ id: string; type: string; amountMicros: string; balanceAfterMicros: string; reason: string; createdAt: string }> };
